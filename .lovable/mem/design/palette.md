---
name: Brand palette — Ocean Wellness
description: Goodfill Samui palette tokens; ocean blue + aqua mint + ivory + sand-gold
type: design
---
Active palette: **Ocean Wellness** (Koh Samui).

- `--background` ivory cream `#FFF8EC`
- `--foreground` / `--navy` / `--emerald-deep` deep ocean `#0C3B4A`
- `--primary` / `--primary-teal` / `--emerald` ocean blue `#1E88A8`
- `--teal-accent` / `--sea` sea blue `#2E8BC0`
- `--accent` / `--mint` aqua mint `#7FE3D0`
- `--secondary` / `--pale-mint` soft sky `#DDF6FF`
- `--gold` sand-gold `#C9A24A`; `--gold-soft` `#EFE3D0`; `--cream` champagne `#FFF1C7`
- `--coral` sunset peach `#E89A6B`

Legacy class names (text-emerald, bg-navy, text-mint, text-gold, text-sea) remap to the ocean equivalents via :root tokens — do not introduce raw hex in components; use semantic tokens.