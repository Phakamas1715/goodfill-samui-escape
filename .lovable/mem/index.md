# Project Memory

## Core
Active palette: Ocean Wellness — ivory `#FFF8EC` base, ocean blue `#1E88A8` primary, aqua mint `#7FE3D0` accent, sand-gold `#C9A24A` highlight, deep ocean `#0C3B4A` ink. Never write raw hex in components; use semantic tokens in src/styles.css.

## Memories
- [Palette tokens](mem://design/palette) — Ocean Wellness token mapping + legacy class remaps